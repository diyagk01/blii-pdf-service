# Google Cloud App Engine configuration
runtime: custom
env: flex

automatic_scaling:
  min_num_instances: 0
  max_num_instances: 2
  cool_down_period: 120s
  cpu_utilization:
    target_utilization: 0.6

resources:
  cpu: 1
  memory_gb: 1
  disk_size_gb: 10

env_variables:
  PYTHONUNBUFFERED: 1
